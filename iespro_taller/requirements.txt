pymysql>=1.1.0
ollama>=0.4.0
chromadb>=0.5.0
SpeechRecognition>=3.10.0
websockets>=12.0
vosk>=0.3.44
sounddevice>=0.5.0

# API web y Docker (Semana 6)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9
httpx>=0.27.0

# Advanced RAG (Semana 7)
rank-bm25>=0.2.2

# Evaluador automatizado
fpdf2>=2.8.0

# Reranker opcional (más preciso; requiere torch)
# sentence-transformers>=3.0.0

# Micrófono: sounddevice (Windows/Python 3.14+, incluye PortAudio). Mac alternativa: pyaudio + brew install portaudio
# openai-whisper + torch son opcionales (voz local); sin ellos usa STT alternativo con internet
# vosk descarga automáticamente vosk-model-small-es-0.42 (~39MB) la primera vez que usas Voz
