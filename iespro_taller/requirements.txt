pymysql>=1.1.0
ollama>=0.4.0
chromadb>=0.5.0
SpeechRecognition>=3.10.0
# pyaudio: en Mac instalar antes brew install portaudio y luego pip install pyaudio
# openai-whisper + torch son opcionales (voz local); sin ellos usa STT alternativo con internet
